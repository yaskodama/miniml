# MINICAML2 — 拡張版 MINICAML インタプリタ

Caml Light 0.71 付属の MINICAML を 1996 年に Standard ML へ移植した
`minicaml.sml` を出発点に、構文を拡張し、現代の Standard ML 処理系で
動くようにしたものです。

## ファイル

| ファイル | 内容 |
|---|---|
| `minicaml.sml` | 1996 年版（原典、無変更） |
| `minicaml2.sml` | 拡張版インタプリタ本体 |
| `demo.mml` | 拡張構文を一巡するデモプログラム |
| `newton.mml` | ニュートン法（整数平方根・固定小数点・高階関数版） |
| `hanoi.mml` | ハノイの塔（手数・手順リスト・印字・盤面シミュレーション） |
| `samples/` | サンプルプログラム 10 本（下記） |
| `minicaml2` | `polyc` が作る実行ファイル（`make` で生成、リポジトリには含めない） |
| `Makefile` | ビルドと実行 |

## 使い方

```sh
brew install polyml          # 未導入なら

make                         # minicaml2 をビルド
make demo                    # demo.mml を実行
./minicaml2 newton.mml       # サンプル: ニュートン法
./minicaml2 hanoi.mml        # サンプル: ハノイの塔
make samples                 # samples/ の 10 本をまとめて実行
./minicaml2                  # 対話モード
./minicaml2 demo.mml         # ファイルを実行
```

Poly/ML の REPL から使うこともできます。

```sh
poly -q --use minicaml2.sml
> run ();                    (* 対話モード *)
> runFile "demo.mml";        (* ファイル実行 *)
> trace := true;             (* 構文木のダンプを有効化 *)
```

フレーズの終端は原版どおり単一のセミコロン `;` です。角括弧の中の `;`
はリストの区切りとして扱われるので `[1; 2; 3];` と書けます。

## 1996 年版からの変更点

### 移植

原版は当時の SML/NJ 独自 API（`lookahead` / `input` / `std_in` /
多相 `print` / `inc` / `dec` / `nth`）に依存していました。すべて標準
Basis Library に置き換えたので、Poly/ML・MLton・SML/NJ で動きます。

字句解析器を独立させ、構文解析は「先読み 1 トークンを可変セルに持つ
再帰下降」に作り替えました。原版のように先読みトークンを全生成規則へ
手で引き回す必要がなくなり、構文の追加が容易になっています。

### 追加した構文

| 機能 | 例 |
|---|---|
| 条件式 | `if n = 0 then 1 else n * fac (n-1)` |
| リストリテラル | `[1; 2; 3]`, `[]` |
| リストパターン | `[]`, `x :: r`, `[a; b]` |
| ワイルドカード | `_` |
| 括弧付きパターン | `(0, _)`, `((a, b), c)` |
| 関数定義の糖衣 | `let f x y = ...`, `let rec quicksort l = ...` |
| 無名関数 | `fun x -> x * x`, `fun a b -> a + b` |
| 式としての let | `let s = a * a in s + 1`（任意の深さでネスト可） |
| 短絡論理演算 | `&&`, `\|\|` |
| 文字列 | `"a\nb"`, `^`, `string_of_int`, `print_string` |
| 単項マイナス | `-7`, `2 * -3` |
| 剰余 | `n mod 4` |
| ネスト可能なコメント | `(* a (* b *) c *)` |

### 型と組み込み

- `=` `<>` `<` `>` `<=` `>=` を多相化しました。整数・文字列・真偽値・
  unit・組・リストを構造的に比較できます（`[1;2;3] < [1;2;4]` など）。
- 多相な組み込み `hd` `tl` `fst` `snd` を追加しました。
- **整数は多倍長**（`IntInf`）です。桁溢れが無いので `2^64-1` も
  `100!` もそのまま計算できます。リテラルも桁数無制限です。
  インタプリタ内部（型の水準、リストの長さなど）は処理系の
  固定長 `int` のままで、object 言語の数だけを `IntInf.int` に
  しています。
- そのほか `^` `mod` `string_of_int` `print_string` `print_int`
  `print_newline` を追加。原版の `not` `write_int` `quit` は残しています。

### 表示と診断

- リストは `[1; 2; 3]` の形で表示します（原版は `1::2::[]`）。
- 負の整数は Caml 流に `-1` と表示します（SML の `~1` ではありません）。
- 文字列はエスケープして表示します。
- 型変数が 27 個を超えても破綻しません。
- 構文エラーは「何を期待して何が来たか」を報告し、次の `;` まで
  読み飛ばして復帰します。原版はエラーの種類しか出しませんでした。
- どんなプログラムを食わせてもインタプリタが死なないように、
  処理系側の例外（`Overflow` / `Div` ほか）まで含めて捕捉します。

## OCaml との意味論の対応

`mod` は OCaml に合わせ、剰余の符号が被除数に従います
（`(-7) mod 3 = -1`）。SML の `mod` は床剰余なので `2` になりますが、
実装では `Int.rem` を使っています。

**既知の相違**：除算 `/` は SML の `div`（床除算）のままです。
OCaml の `/` は 0 方向への切り捨てなので、負数で結果が食い違います。

| 式 | MINICAML2 | OCaml |
|---|---|---|
| `(-7) / 3` | `-3` | `-2` |
| `(-7) mod 3` | `-1` | `-1` |
| `(-7)/3*3 + (-7) mod 3` | `-10` | `-7` |

`mod` だけを OCaml 準拠にしたため、`a = (a/b)*b + a mod b` の恒等式が
負数で成立しません。`/` も揃えるには `minicaml2.sml` の
`val_env_initial` にある

```sml
("/",   prim_arith "/" (safe_div "/" (op div))),
```

を `Int.quot` に変えるだけです。

## サンプルプログラム

`samples/` に 10 本あります。`make samples` で全部走ります。
結果は Python で独立に計算して突き合わせ済みです。

| ファイル | 内容 |
|---|---|
| `01-fizzbuzz.mml` | FizzBuzz。値として作る版と印字する版 |
| `02-primes.mml` | エラトステネスの篩、試し割り、双子素数、素因数分解 |
| `03-sort.mml` | 挿入・マージ・クイックの三種と、一致するかの検算 |
| `04-bignum.mml` | `100!`、`fib 500`、二項係数、冪剰余、フェルマーの小定理 |
| `05-rational.mml` | 有理数を組で表し、約分しながら厳密に計算する |
| `06-queens.mml` | N クイーン。解の個数と盤面の描画 |
| `07-collatz.mml` | コラッツ予想。列の長さ、最大値、最長の種を探す |
| `08-pascal.mml` | パスカルの三角形。行和が `2^n` になることの確認 |
| `09-rpn.mml` | 逆ポーランド計算機。この言語で書いた小さなインタプリタ |
| `10-life.mml` | ライフゲーム。ブリンカー・ブロック・グライダー |

### 書くときに当たる制約

- **相互再帰が書けません。** `let rec` は一つの関数しか束縛しないので、
  `03-sort.mml` の `merge` のように対を受け取る形へ畳んでください。
- **式を並べる `;` がありません。** 印字を続けたいときは
  `let u = print_string ... in ...` で評価順を作ります。
  `let _ = ...` は名前の位置に識別子が要るので使えません。
- **文字列は `^` と比較だけです。** 長さも添字もありません。
- **代数的データ型がありません。** `09-rpn.mml` では記号を
  `(種別, 値)` の組で表し、`"n"` というタグで数と演算子を区別しています。
- **ぶら下がり `else`** は内側を括弧でくくってください
  （`if a then (if b then x else y) else z`）。

## サンプルについての注意

### 式を並べる `;` が無い

原版から引き継いだ文法にはシーケンス式がありません。印字を並べたい
ときは `let` で捨て値に束縛して評価順を作ります。

```
let _sub1 = hanoi (n - 1) a c b in
let _move = print_string "..." in
hanoi (n - 1) c b a
```

### 整数ニュートン法は根の 1 つ上で止まりうる

割り算が切り捨てなので、`x^2 = 1234321` の根 `1111` に対し `x = 1112`
では `f(x)/f'(x) = 2223/2224 = 0` となり、更新が止まります。
`newton.mml` では止まった点から `f(x) <= 0` まで 1 ずつ降ろす
`descend` を足して仕上げています。

## 著作権

`minicaml.sml` は Copyright (c) 1996 Yasushi KODAMA、その原典は
Caml Light 0.71 付属の MINICAML（Copyright INRIA）です。`minicaml2.sml`
も同じ条件に従います。すなわち、利用・改変・組み込みは自由ですが、
派生物は同じ条件で配布し、この表示を保持してください。
